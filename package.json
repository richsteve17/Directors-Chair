{
  "name": "cmass-gauntlet",
  "version": "3.0.0",
  "private": true,
  "description": "CMASS — The Director's Gauntlet. Vite SPA + a Vercel serverless Gemini proxy (key stays server-side).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:vercel": "vercel dev",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
